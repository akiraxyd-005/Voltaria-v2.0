// Legacy handler stub - kept for backward compatibility
// Main handling is done by handlers/message-handler.js and handlers/command-handler.js

module.exports = {
    handleMessage: async (sock, msg) => {
        // No-op fallback handler
    }
};
